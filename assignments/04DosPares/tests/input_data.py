# IMPORTANT: The length of the lists has to be the same

# List of lists, where each of the inner lists has all the inputs for a single test
input_values = [
    # Test case 1
    (
        #Inputs
        ["42"],
        # Ouputs
        ["", "0.046"],
        # Message in case of failure
        ["Error al detectar 2 pares, no debes modificar el mazo ni la mano"]
    ),
    # Test case 2
    (
        #Inputs
        ["1"],
        # Ouputs
        ["", "0.049"],
        # Message in case of failure
        ["Error al detectar 2 pares, no debes modificar el mazo ni la mano"]
    ),
    # Test case 3
    (
        #Inputs
        ["2"],
        # Ouputs
        ["", "0.043"],
        # Message in case of failure
        ["Error al detectar 2 pares, no debes modificar el mazo ni la mano"]
    ),
    # Test case 4
    (
        #Inputs
        ["3"],
        # Ouputs
        ["", "0.045"],
        # Message in case of failure
        ["Error al detectar 2 pares, no debes modificar el mazo ni la mano"]
    )
    
]

